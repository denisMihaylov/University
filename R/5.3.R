library("Simple")
data(chicken)
attach(chicken)
boxplot(chicken, main="Chicken Ratio Boxplot")
detach(chicken)

# Определено се забелязва разликата между средните стойности при използването на различните ражби
